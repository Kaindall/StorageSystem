server:
  port: 8081
  servlet:
    context-path: /orders
spring:
  application:
    name: orders
  datasource:
    url: jdbc:mysql://mysql:3306/STORAGE?useSSL=false&serverTimezone=UTC
    username: Kaindall
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  main:
    allow-bean-definition-overriding: true
kafka:
  url: kafka:9092
  producer:
    topic: movement-qeue
  consumer:
    topic: movement-result
    group-id: movements-consumer-1
clients:
  movements:
    url: http://products:8080
